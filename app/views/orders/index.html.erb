<div class='container mt-5'>
  <h1>All orders</h1>
  <div class='border'>
    <% @orders.each do |order| %>
      <%= link_to order_path(order) do%>
        <%= order.id %>
      <% end %>
    <% end %>
  </div>
</div>
