<div class='container mt-5'>
  <h1>All orders</h1>
    <% @orders.each do |order| %>
      <div class='border'>
        <%= link_to order_path(order) do%>
          <%= order.id %>
        <% end %>
      </div>
    <% end %>
</div>
